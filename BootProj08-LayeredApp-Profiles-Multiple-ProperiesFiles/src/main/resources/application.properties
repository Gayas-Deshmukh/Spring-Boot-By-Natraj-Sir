spring.application.name=BootProj04-LayeredApp-MySQL

#Profile
spring.profiles.active=stage

#DataSource Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/spring-boot(natraj)
spring.datasource.username=root
spring.datasource.password=root

#spring.profiles.include=dev

#Default it will comment to Hikari CP

#Profile - Dev = Connect to topcat cp

#Profile - test = Connect to apache dbcp2

#Profile - uat = Connect to c3p0